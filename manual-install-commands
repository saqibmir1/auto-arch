cfdisk (make partitions)
timedatectl set-ntp true
#set parallel downloads = 5

mkfs.ext4 $rootpartition
mkswap $swappartition
mkfs.fat -F 32 $bootpartition

mount $rootpartition /mnt
swapon $swappartition
mkdir -p /mnt/boot/efi
mount $bootpartition /mnt/boot/efi

pacstrap /mnt base linux linux-firmware grub efibootmgr networkmanager linux-headers sof-firmware base-devel nano amd-ucode archlinux-keyring
genfstab -U /mnt >> /mnt/etc/fstab
arch-chroot /mnt

ln -sf /usr/share/zoneinfo/$timezone /etc/localtime
hwclock --systohc
uncomment "en_US.UTF-8 UTF-8" in /etc/locale.gen
locale-gen
echo "LANG=en-us.UTF-8 > /etc/locale.conf
echo "arch" > /etc/hostname
passwd
useradd -m -G wheel -s /bin/bash sisyphus
EDITOR=nano visudo

grub-install /dev/nvme0n1
grub-mkconfig -o /boot/grub/grub.cfg

systemctl enable NetworkManager

exit
umount -a
reboot

------------------------------------------------------------------------------------------

sudo pacman -Syu
do i love candy, color, parallel download in pacman.conf
sudo sed -i "s/-j2/-j$(nproc)/;/^#MAKEFLAGS/s/^#//" /etc/makepkg.conf

sudo pacman -S sddm konsole plasma
sudo systemctl enable sddm
reboot

------------------------------------------------------------------------------------------

sudo pacman -S android-tools bleachbit firefox htop neovim mpv ranger telegram-desktop zsh zsh-syntax-highlighting jq fzf bluedevil bluez-utils git noto-fonts-emoji cmus yt-dlp wget gwenview okular spectacle --needed xf86-video-amdgpu
git clone https://aur.archlinux.org/yay-bin.git and makepkg -frsi
yay -S ytfzf auto-cpufreq --removemake

sudo systemctl enable bluetooth
sudo systemctl enable auto-cpufreq.service
sudo systemctl enable sddm

configure grub for dual boot(install os-prober,ntfs-3g) and quiet and remake grub cfg
reboot

------------------------------------------------------------------------------------------

balooctl suspend
balooctl disable
rm -rf ~/.local/share/baloo

git clone https://github.com/saqibmir1/walpapers.git ~/Pictures
git clone https://github.com/saqibmir1/dotfiles.git

move dotfiles

chsh -s $(which zsh)
mkdir -p ~/.cache/zsh

mkdir Code Git Projects
rmdir Templates Public
rm -rf yay-bin
sudo pacman -Rns discover

ssh-keygen -t ed25519 -C "mirsaquib3737@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
